# 合成回归语料

`synthetic` 中的 `.out` 文件由 `tools\generate_synthetic_outputs.py` 确定性生成，只用于解析器、GUI 和 Excel 报告测试。

这些文件不是 MCNP 物理计算结果，不得用于剂量、屏蔽、临界安全或工程判断。它们也不是模型训练得到的数据；当前软件不会在本地训练大模型，而是通过规则解析事实，并可选调用用户配置的 AI 接口生成中文解释。

语料覆盖正常低误差、高误差与零分箱、高精度稳定收敛、截断、错误版本和未知表等情况。`synthetic\manifest.json` 保存每个场景的预期值。
